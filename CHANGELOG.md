# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2026-06-23

### Added
- React/Zustand based modern frontend dashboard.
- Extensive REST and WebSocket API documentation.
- Enterprise-grade sentiment and churn risk AI engines.
- Role-based access control (RBAC) on keywords and alerts.
- Thread-safe model loading mechanism for AI endpoints.
- Secure WebSocket endpoint for live ASR streams.
- MIT License.

### Changed
- Migrated legacy scripts to a structured FastAPI and Streamlit hybrid architecture.
- Optimized Docker builds with multi-stage configuration and `.dockerignore`.
- Enhanced test coverage using `pytest-cov`, with conditional skipping for ML-heavy operations.

### Fixed
- Addressed JWT hardcoded secret vulnerability and replaced plain-text user passwords with bcrypt hashing.
- Fixed `DetachedInstanceError` related to `SessionLocal` context management.
\n- **feat(core): initialize batched inference pipeline for higher throughput**: Implement faster-whisper batched inference to support high-volume concurrent requests.\n- **perf(engine): optimize MLX greedy decoder phonetic tokenization**: Enhance MLX token selection to reduce phonetically similar word errors in Turkish.\n- **fix(ui): resolve state mismatch in real-time transcription dashboard**: Fix a race condition in Streamlit UI where partial transcripts would overwrite finalized ones.\n- **refactor(deps): migrate from torch 1.13 to torch 2.x for better MPS support**: Upgrade pytorch dependencies to leverage native Apple Silicon acceleration.\n- **feat(asr): add support for dynamic hotword injection**: Allow passing domain-specific terminology on the fly without retraining the acoustic model.\n- **docs(api): update swagger documentation for audio streaming endpoints**: Add detailed parameter descriptions and response schemas for the WebSocket streaming API.\n- **test(engine): add unit tests for empathy and sentiment analysis engines**: Introduce comprehensive test coverage for NLP engines to prevent future regressions.\n- **chore(docker): optimize multi-stage build for frontend**: Reduce docker image size by 40% using alpine base and multi-stage builds.\n- **feat(nlp): implement real-time sentiment tracking**: Add continuous sentiment analysis over audio chunks to track caller mood changes.\n- **fix(api): handle malformed audio headers gracefully**: Prevent backend crash when receiving corrupted or unsupported audio formats.\n- **perf(db): add redis caching for repetitive audio segment processing**: Implement in-memory caching to avoid re-transcribing identical audio snippets.\n- **style(ui): apply modern glassmorphism design to main dashboard**: Update CSS and React components to match the new corporate design language.\n- **refactor(core): decouple audio chunking logic from transcription engine**: Move chunking algorithms to a dedicated preprocessing module for better testability.\n- **feat(analytics): add speaker diarization confidence scores**: Expose model confidence metrics for speaker identification in the reporting dashboard.\n- **fix(docker): resolve volume mount permission issues on linux hosts**: Fix uid/gid mapping in docker-compose for smoother local development setups.\n- **docs(readme): add comprehensive setup guide for macos**: Provide step-by-step instructions for setting up the local dev environment on Apple Silicon.\n- **perf(asr): implement early stopping for silent audio chunks**: Skip model inference on pure silence to save GPU cycles and reduce latency.\n- **feat(ui): add dark mode support to the legacy streamlit app**: Implement a custom theme toggle for better visibility in low-light environments.\n- **chore(deps): update fastAPI and uvicorn to latest stable versions**: Keep web framework dependencies up to date for security and performance.\n- **fix(core): mitigate memory leak in long-running streaming sessions**: Force garbage collection and release GPU memory tensors periodically during streams.\n- **feat(db): add PostgreSQL integration for transcript persistence**: Move from sqlite to postgres for production-ready data storage.\n- **perf(nlp): parallelize sentiment and compliance engines**: Run NLP processing steps asynchronously to reduce overall pipeline latency.\n- **refactor(api): standardize error response formats across all endpoints**: Use RFC 7807 problem details format for consistent error handling in clients.\n- **feat(auth): implement JWT based authentication for API routes**: Secure transcription endpoints with standard bearer token authentication.\n- **fix(ui): correct word-level timestamp alignment in subtitle export**: Fix an issue where SRT file generation would misalign words by a few milliseconds.\n- **docs(arch): add system architecture diagrams**: Include Mermaid.js diagrams detailing the microservice communication flow.\n- **chore(ci): set up github actions for automated testing**: Run unit tests and linting on every pull request to maintain code quality.\n- **feat(asr): support multi-language fallback detection**: Automatically switch transcription models if the primary language confidence is low.\n- **perf(engine): reduce whisper model memory footprint by 15%**: Apply int8 quantization to non-critical model layers without sacrificing accuracy.\n- **fix(db): resolve connection pool exhaustion under high load**: Configure SQLAlchemy pool size and timeout settings appropriately.\n- **style(code): run black and isort across the entire python codebase**: Enforce PEP8 styling and consistent import ordering.\n- **refactor(ui): extract reusable components in the React frontend**: Clean up the main dashboard page by moving logic into smaller, focused components.\n- **feat(analytics): add export to CSV functionality for call reports**: Allow supervisors to download transcription and sentiment data for offline analysis.\n- **chore(deps): remove unused legacy libraries**: Clean up requirements.txt by removing obsolete dependencies.\n- **fix(asr): fix edge case where very short audio files would crash**: Add padding to audio files shorter than the minimum whisper context length.\n- **feat(nlp): introduce custom named entity recognition for product names**: Train and integrate a small spaCy model to extract company-specific products.\n- **perf(api): compress HTTP responses using gzip middleware**: Reduce bandwidth usage for large transcription JSON payloads.\n- **docs(deploy): add kubernetes deployment manifests**: Provide sample yaml files for deploying the application stack to a k8s cluster.\n- **feat(core): add audio normalization preprocessing step**: Automatically adjust volume levels of incoming audio before transcription.\n- **fix(ui): fix responsive layout issues on mobile devices**: Ensure the dashboard is usable on smaller screens by adjusting CSS grid layouts.\n- **refactor(engine): abstract out model loading mechanism**: Create a generic interface to easily swap between different ASR backends (e.g., Whisper, Wav2Vec).\n- **feat(auth): add role-based access control (RBAC)**: Differentiate between admin, supervisor, and agent user roles.\n- **perf(db): add database indexes on frequently queried fields**: Speed up transcript search and filtering operations.\n- **chore(git): update .gitignore to exclude local IDE and coverage files**: Keep the repository clean from environment-specific artifacts.\n- **fix(nlp): handle empty transcripts in sentiment engine**: Prevent zero-division errors when analyzing completely silent recordings.\n- **feat(ui): implement real-time audio waveform visualization**: Add a visual indicator of audio activity during live transcription sessions.\n- **docs(api): add postman collection for easy API testing**: Export and include a collection of common API requests for developers.\n- **perf(asr): implement batched decoding for offline processing**: Process historical audio files in large batches to maximize GPU utilization.\n- **refactor(core): use Pydantic models for internal data structures**: Ensure strong typing and validation throughout the application logic.