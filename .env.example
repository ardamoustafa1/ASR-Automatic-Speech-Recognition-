# ─── Security ────────────────────────────────────────────────────────────────
# REQUIRED: Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ASR_JWT_SECRET_KEY=change_this_to_a_secure_random_string_at_least_32_chars

# REQUIRED: Set a strong admin password (min 12 chars recommended)
ASR_ADMIN_PASSWORD=change_this_to_a_strong_password

# ─── Churn Risk Engine ────────────────────────────────────────────────────────
# Comma-separated name(s) of the company deploying this system (your client),
# case-insensitive. Excluded from competitor detection so an agent saying
# "X'ten arıyorum" on their own company's outbound calls isn't scored as a
# competitor mention. e.g. for a Vodafone deployment: ASR_CHURN_OWN_COMPANY_NAMES=vodafone
ASR_CHURN_OWN_COMPANY_NAMES=

# ─── Database ────────────────────────────────────────────────────────────────
# SQLite (Development)
ASR_DATABASE_URL=sqlite:///./data/asr_pro.db

# PostgreSQL (Production) — uncomment and fill in your credentials:
# ASR_DATABASE_URL=postgresql://user:password@localhost:5432/asr_pro

# ─── PostgreSQL Container Credentials (docker-compose) ───────────────────────
POSTGRES_USER=asr_user
POSTGRES_PASSWORD=change_this_strong_db_password
POSTGRES_DB=asr_pro

# ─── Cache / Queue ───────────────────────────────────────────────────────────
# Leave empty to use in-memory cache (development)
# For production, set to your Redis URL:
# ASR_REDIS_URL=redis://localhost:6379/0
ASR_REDIS_URL=

# ─── Webhooks ────────────────────────────────────────────────────────────────
# URL to receive alert payloads when a critical threshold is crossed (POST):
# ASR_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
ASR_WEBHOOK_URL=

# ─── CORS ────────────────────────────────────────────────────────────────────
ASR_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ─── Frontend ────────────────────────────────────────────────────────────────
# Used at Vite build time. For docker-compose local use, keep localhost because
# requests originate from the user's browser, not from the frontend container.
VITE_API_URL=http://localhost:8000/api/v1

# ─── ASR Engine ──────────────────────────────────────────────────────────────
# Whisper model checkpoint. "large-v3" (default). "large-v3-turbo" measured
# faster AND more accurate on our clean benchmark but had a real
# hallucination on noisy real audio that large-v3 didn't - see
# .benchmarks/results.md and verify on your own recordings before switching.
# ASR_ASR_MODEL_SIZE=large-v3

# ─── PII Redaction (KVKK / PCI-DSS) ──────────────────────────────────────────
# When true (default), checksum-validated personal identifiers spoken during
# calls (TCKN, credit card PAN, Turkish IBAN, mobile numbers) are masked
# before transcripts are persisted. Required posture for bank deployments
# (PCI-DSS 3.4 forbids storing card numbers). Set to false only for
# lab/debug environments.
# ASR_PII_REDACTION_ENABLED=true

# ─── Testing ─────────────────────────────────────────────────────────────────
# Set to 1 to skip downloading heavy HuggingFace models during tests
ASR_TEST_NO_MODEL=0

# ─── Hugging Face / AI Models ────────────────────────────────────────────────
# Required for pyannote.audio speaker diarization (Gated Model Access)
HUGGING_FACE_HUB_TOKEN=your_huggingface_access_token_here
HF_TOKEN=your_huggingface_access_token_here
